# Localization Data

Generated by `pwsh -File scripts/tools/extract_localization.ps1`.

- `en.json` contains extracted English/source strings and generated placeholders for existing localization keys.
- `ru.json` contains the same keys with empty values, ready for translation.
- `_sources.json` maps every key back to the file/property/line it came from.

Runtime loading is handled by `scripts/infrastructure/localization.gd`. It registers these JSON files as Godot `Translation` resources through `TranslationServer`.

## Hard rule for player-facing text

Do not add player-facing hardcoded text to scenes, scripts, data files, UI panels, popups, toasts, tooltips, dialogue configs, maps, enemies, skills, or editor/gameplay screens.

Every visible string must have a stable localization key and that key must exist in both:

- `data/localization/en.json`
- `data/localization/ru.json`

Allowed exceptions are values that are genuinely language-neutral and will never need translation, such as pure numbers, timers, percentages, coordinates, icon-only glyphs, file paths, ids, enum values, debug-only log categories, and other internal tokens.

Scene files should store localization keys in text-like properties, for example:

```text
text = "ui_pause_menu_resume_button_text"
tooltip_text = "ui_top_hud_bar_help_button_tooltip_text"
```

GDScript should use explicit lookup:

```gdscript
label.text = Localization.t("ui_common_save", "Save")
toast(Localization.tf("ui_score_total", [score], "Total score: %d"))
```

When adding a new interface, window, button, tooltip, toast, prompt, dialogue line, map title, enemy text, or skill text, add the key to both locale files in the same change. Do not rely on English/source text literals as the long-term localization source.

## Adding new localization keys

Any new localization key must be added to both localization files immediately:

- `en.json`
- `ru.json`

Do not leave a key only in code, scenes, configs, enemies, skills, maps, dialogues, or any other source file. If a new localized string is introduced, add its key to both JSON files in the same change.

Both files must always contain the same set of keys. This keeps the localization table, parser, and runtime translations in sync.

## Localization key naming rules

Use stable, descriptive, snake_case keys.

Prefer keys that describe where the text is used and what kind of text it is.

Good examples:

- `stapler_staple_shot_name`
- `stapler_staple_shot_desc`
- `stapler_staple_shot_tooltip`
- `status_burning_name`
- `status_burning_desc`
- `ui_main_menu_start_button_text`
- `ui_pause_restart_title`
- `dialogues_merchant_greet_text`

Avoid vague or unstable keys:

- `text1`
- `new_label`
- `button`
- `enemy_name`
- `tmp_desc`
- `test_string`

Keys should stay valid even if the displayed text changes later.

## Recommended key structure

Use prefixes by content type.

### Skills

```text
<skill_id>_name
<skill_id>_desc
<skill_id>_tooltip
```

Example:

```text
bee_sting_name
bee_sting_desc
bee_sting_tooltip
```

### Enemies

```text
<enemy_id>_name
<enemy_id>_desc
```

Example:

```text
burning_bear_name
burning_bear_desc
```

### Status effects

```text
status_<status_id>_name
status_<status_id>_desc
```

Example:

```text
status_poisoned_name
status_poisoned_desc
```

### UI strings

Use the screen, panel, or system name first, then the element purpose.

```text
ui_<screen_or_panel>_<element>_<property>
```

Examples:

```text
ui_main_menu_start_button_text
ui_settings_panel_language_label_text
ui_pause_restart_confirm
ui_map_editor_save_error
```

For common reusable UI labels, use:

```text
ui_common_<action_or_label>
```

Examples:

```text
ui_common_save
ui_common_cancel
ui_common_delete
```

### Dialogues

Use the dialogue group and line purpose.

```text
dialogues_<group>_<line_id>_text
dialogues_<group>_<line_id>_choices_<index>_label
dialogues_speakers_<speaker_id>_display_name
```

Examples:

```text
dialogues_merchant_greet_text
dialogues_merchant_greet_choices_0_label
dialogues_speakers_merchant_display_name
```

### Maps and games

```text
maps_<map_id>_name
games_<game_id>_name
games_<game_id>_levels_<index>_display_name
```

Examples:

```text
maps_sample_waves_name
games_sample_game_name
games_sample_game_levels_0_display_name
```

## Text value rules

When adding or editing localized values:

- Preserve placeholders exactly: `%s`, `%d`, `%.2f`, etc.
- Preserve placeholder order unless the formatting code explicitly supports reordering.
- Preserve BBCode tags such as `[b]`, `[color]`, `[/color]`.
- Preserve service symbols such as `★`, `→`, `—`, `…`, `II`, `✕`.
- Preserve intentional line breaks.
- Preserve HTML-like markup such as `<br>` when it is used by UI text.
- Do not translate internal ids, file names, enum values, or debug-only tokens unless they are meant to be shown to the player.

Examples:

```json
"ui_top_hud_turn": "Turn: %d",
"ui_skill_headline_damage": "%s — %d dmg",
"ui_game_editor_overwrite_body": "%s already exists.<br><br>Replace it?"
```

The Russian version must keep the same placeholders and markup:

```json
"ui_top_hud_turn": "Ход: %d",
"ui_skill_headline_damage": "%s — %d урн.",
"ui_game_editor_overwrite_body": "%s уже существует.<br><br>Заменить?"
```

### Skill text variables

Skill description and tooltip strings (`<skill_id>_desc`, `<skill_id>_tooltip`)
may use `$name$` variables. They are resolved by
`SkillFormatter.format_skill_desc(skill)` and
`SkillFormatter.format_skill_human(skill)` from the current skill config, after
level scaling and parameter modifiers.

Common variables:

- `$damage$`, `$heal$`, `$cooldown$`, `$range$`
- `$radius$`, `$area_radius$`, `$max_chain_length$`, `$chain_targets$`
- `$duration$`, `$status_duration$`, `$status_value$`
- Status-specific variables from `data/status_effects/*.json:param_names`,
  for example `$n_attack_buff$`, `$n_attack_debuff$`,
  `$poisoned_damage_pct$`, `$burning_damage$`

Unknown variables are left unchanged so missing support is visible in-game.

Skill descriptions and tooltips may also use plural blocks tied to a numeric variable:

```text
{$chain_targets$;target;targets;targets}
{$duration$;ход;хода;ходов}
```

Syntax:

```text
{$variable$;one;few;many}
```

Keep the numeric `$variable$` inside the block; it renders both the number and
the selected word form.

Examples:

```text
Deals damage to {$chain_targets$;target;targets;targets}.
Оглушает на {$duration$;ход;хода;ходов}.
```

Russian uses:

- `one`: 1, 21, 31, etc., except 11.
- `few`: 2, 3, 4, 22, 23, 24, etc., except 12, 13, 14.
- `many`: 0, 5-20, 25-30, and other numbers ending in 0 or 5-9.

English uses:

- `one`: exactly 1.
- `many`: all other values.

For English, write the same syntax and usually duplicate the plural form in
`few` and `many`, for example `{$duration$;turn;turns;turns}`.

### Runtime format variables

UI/gameplay strings that use `Localization.tf(key, args, fallback)` receive
runtime values from scripts. Keep the same printf-style placeholders in
`en.json` and `ru.json`.

Current runtime placeholders:

- `ui_top_hud_turn`: `%d` = current turn.
- `ui_top_hud_wave`: `%d` = current wave.
- `ui_top_hud_wave_total`: first `%d` = current wave, second `%d` = total waves.
- `ui_player_status_hp_value`: first `%d` = current player HP, second `%d` = max player HP.
- `ui_enemy_details_hp`: first `%d` = current enemy HP, second `%d` = max enemy HP.
- `ui_settings_percent_value`: `%d` = slider value as percent.
- `ui_settings_speed_value`: `%.2f` = game speed multiplier.
- `ui_keybind_cast_slots`: first `%d` = first cast slot number, second `%d` = last cast slot number from `SlotBar.SLOT_COUNT`.
- `ui_keybind_move_step`: `%d` = RMB movement step count.
- `ui_keybind_spawn_dummy`: `%d` = debug dummy spawn count.
- `ui_campaign_end_total_score`: `%d` = final campaign score.
- `skill_offer.mode.upgrade`: `%d` = next skill level.
- `skill_offer.replace.prompt`: `%s` = offered skill display name.

Developer/editor strings also use the same mechanism for counts, file names,
ids, ranges, and validation messages. When adding a new formatted text, document
the key here if it can appear in player-facing UI.

## English and Russian file requirements

When adding a new key:

1. Add the key to `en.json`.
2. Add the same key to `ru.json`.
3. Put the English/source text in `en.json`.
4. Put the Russian translation in `ru.json`, or an empty string if the translation is not ready yet.
5. Re-run the extractor if the key was introduced through source content.

Valid temporary state:

```json
// en.json
"ui_new_feature_title": "New Feature"

// ru.json
"ui_new_feature_title": ""
```

Invalid state:

```json
// en.json
"ui_new_feature_title": "New Feature"

// ru.json
// missing key
```

## Workflow

After adding or renaming content defs, re-run the extractor:

```pwsh
pwsh -File scripts/tools/extract_localization.ps1
```

Then check that:

- `en.json` and `ru.json` contain the same keys.
- `_sources.json` points each extracted key to the correct source file/property/line.
- New keys follow the naming rules above.
- Placeholder sets match between languages.
- No player-facing text remains hardcoded outside localization.
