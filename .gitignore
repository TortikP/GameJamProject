# Godot 4+ specific ignores
.godot/
*.uid
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# 020 — map editor transients (debounced autosave + Playtest snapshot).
# These are dev-loop scratch state, never meant to be committed.
data/maps/__autosave__.json
data/maps/__playtest__.json
